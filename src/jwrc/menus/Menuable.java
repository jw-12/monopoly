package jwrc.menus;

import jwrc.player.Player;

import java.util.ArrayList;


/**
 * Interface that is used by the menus such that they have similar options method
 */
public interface Menuable {
    static void options(Player player, ArrayList<Player> playerList, int amount) {
    }
}
